apiVersion: v1
kind: Service
metadata:
  namespace: wordpress
  name: mysql-service
  labels:
    app: mysql
    tier: database
spec:
  selector:
    app: mysql
    tier: database
  ports:
  - port: 3306
    protocol: TCP
    targetPort: 3306
  type: ClusterIP